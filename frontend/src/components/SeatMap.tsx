import type { Seat } from '../types'

export function SeatMap({ seats, selected, onChange }: { seats: Seat[]; selected: string[]; onChange: (seats: string[]) => void }) {
  function toggle(seat: Seat) { if (seat.status === 'BOOKED') return; if (selected.includes(seat.number)) return onChange(selected.filter((number) => number !== seat.number)); if (selected.length < 6) onChange([...selected, seat.number]) }
  return <section className="seat-section" aria-labelledby="seat-map-title"><div className="seat-heading"><div><p className="eyebrow">Choose up to 6</p><h2 id="seat-map-title">Pick your seats</h2></div><span className="selection-count" aria-live="polite">{selected.length} selected</span></div><div className="seat-legend" aria-label="Seat map legend"><span><i className="seat available" />Available</span><span><i className="seat selected" />Selected</span><span><i className="seat booked" />Booked</span></div><div className="bus-shell"><div className="driver">Driver</div><div className="seat-grid" role="group" aria-label="Bus seat map">{seats.map((seat) => seat.column === 4 ? <div key={`aisle-${seat.id}`} className="aisle" aria-hidden="true" /> : <button key={seat.id} className={`seat ${seat.status === 'BOOKED' ? 'booked' : selected.includes(seat.number) ? 'selected' : 'available'}`} onClick={() => toggle(seat)} disabled={seat.status === 'BOOKED'} aria-pressed={selected.includes(seat.number)} aria-label={`Seat ${seat.number}, ${seat.status === 'BOOKED' ? 'booked' : selected.includes(seat.number) ? 'selected' : 'available'}`}>{seat.number}</button>)}</div></div></section>
}
